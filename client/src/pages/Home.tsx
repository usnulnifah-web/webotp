import { useMemo, useState } from "react";
import { Link, useLocation } from "wouter";
import { ArrowRight, BarChart3, BookOpen, Check, ChevronRight, CircleHelp, Code2, Copy, ExternalLink, KeyRound, LayoutDashboard, LifeBuoy, LogOut, Menu, MoreHorizontal, Play, Plus, RefreshCw, Server, ShieldCheck, SlidersHorizontal, Sparkles, Terminal, Wallet, Webhook, X, Zap } from "lucide-react";
import { toast } from "sonner";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";

const formatIDR = (minor: number | null | undefined) => `Rp ${new Intl.NumberFormat("id-ID").format(minor ?? 0)}`;
const formatDate = (value: Date | string | null | undefined) => value ? new Date(value).toLocaleString("id-ID", { day: "2-digit", month: "short", hour: "2-digit", minute: "2-digit" }) : "—";
const navItems = [
  { key: "dashboard", label: "Overview", icon: LayoutDashboard },
  { key: "buy", label: "Buy number", icon: Zap },
  { key: "activations", label: "Activations", icon: Play },
  { key: "wallet", label: "Wallet & ledger", icon: Wallet },
  { key: "keys", label: "API keys", icon: KeyRound },
  { key: "webhooks", label: "Webhooks", icon: Webhook },
  { key: "docs", label: "API docs", icon: BookOpen },
  { key: "admin", label: "Admin operations", icon: SlidersHorizontal },
];

function Logo({ dark = false }: { dark?: boolean }) {
  return <div className={`flex items-center gap-3 ${dark ? "text-white" : "text-slate-950"}`}><div className="logo-mark"><span>W</span></div><div><div className="text-base font-black tracking-tight">web<span className="text-cyan-500">otp</span></div><div className={`text-[9px] font-semibold uppercase tracking-[0.22em] ${dark ? "text-slate-400" : "text-slate-400"}`}>API infrastructure</div></div></div>;
}

function PublicNav() {
  return <header className="sticky top-0 z-40 border-b border-slate-200/70 bg-white/85 backdrop-blur-xl"><div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-5 lg:px-8"><Link href="/"><Logo /></Link><nav className="hidden items-center gap-7 text-sm font-semibold text-slate-600 lg:flex"><a href="#platform" className="transition hover:text-slate-950">Platform</a><a href="#services" className="transition hover:text-slate-950">Services</a><a href="#developers" className="transition hover:text-slate-950">Developers</a><a href="#status" className="transition hover:text-slate-950">Status</a><a href="#faq" className="transition hover:text-slate-950">FAQ</a></nav><div className="flex items-center gap-3"><button className="hidden text-sm font-bold text-slate-700 sm:block" onClick={() => startLogin()}>Sign in</button><button className="btn-primary px-4 py-2.5 text-sm" onClick={() => startLogin()}>Get started <ArrowRight size={15} /></button></div></div></header>;
}

function PublicHome() {
  const stats = trpc.public.stats.useQuery();
  const catalog = trpc.public.catalog.useQuery();
  const services = catalog.data?.data ?? [];
  return <div className="min-h-screen bg-[#f7f9fc] text-slate-950"><PublicNav /><main>
    <section className="relative overflow-hidden border-b border-slate-200 bg-[#061527] text-white"><div className="absolute inset-0 grid-pattern opacity-40" /><div className="orb orb-cyan" /><div className="orb orb-violet" /><div className="relative mx-auto grid max-w-7xl gap-16 px-5 pb-20 pt-20 lg:grid-cols-[1.05fr_.95fr] lg:items-center lg:px-8 lg:pb-28 lg:pt-28"><div><div className="eyebrow border border-cyan-400/30 bg-cyan-300/10 text-cyan-200"><Sparkles size={13} /> Developer-first OTP infrastructure</div><h1 className="mt-7 max-w-3xl text-5xl font-black leading-[1.02] tracking-[-0.055em] sm:text-6xl lg:text-7xl">Virtual numbers, built for <span className="text-gradient">reliable flows.</span></h1><p className="mt-7 max-w-xl text-lg leading-8 text-slate-300">One API for virtual numbers, OTP delivery, wallet controls, and supplier routing. Ship verification experiences without building the network layer from scratch.</p><div className="mt-9 flex flex-wrap gap-3"><button className="btn-primary" onClick={() => startLogin()}>Launch your workspace <ArrowRight size={17} /></button><a className="btn-ghost-dark" href="#developers"><Code2 size={17} /> Read the docs</a></div><div className="mt-10 flex flex-wrap items-center gap-6 text-xs font-semibold text-slate-400"><span className="flex items-center gap-2"><ShieldCheck size={15} className="text-emerald-300" /> Hash-first API keys</span><span className="flex items-center gap-2"><Zap size={15} className="text-amber-300" /> Idempotent transactions</span><span className="flex items-center gap-2"><Webhook size={15} className="text-cyan-300" /> Signed webhooks</span></div></div><div className="relative"><div className="dashboard-window"><div className="flex items-center justify-between border-b border-white/10 px-5 py-4"><div className="flex gap-1.5"><i /><i /><i /></div><div className="rounded-full border border-emerald-300/20 bg-emerald-300/10 px-3 py-1 text-[10px] font-bold uppercase tracking-widest text-emerald-300">Live network</div></div><div className="p-5"><div className="flex items-end justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.18em] text-slate-500">Requests / 24h</p><p className="mt-2 text-4xl font-black">2.84M</p></div><div className="rounded-xl bg-cyan-400/10 px-3 py-2 text-right"><p className="text-[10px] text-slate-500">Success rate</p><p className="font-bold text-cyan-300">99.2%</p></div></div><div className="mt-6 flex h-28 items-end gap-2">{[35,48,42,65,58,78,70,92,77,88,82,100,94,108,102,120].map((height, index) => <div key={index} className="flex-1 rounded-t-md bg-gradient-to-t from-cyan-500/25 to-cyan-300" style={{ height: `${height / 1.5}%`, opacity: .38 + index / 40 }} />)}</div><div className="mt-5 grid grid-cols-3 gap-3"><div className="mini-metric"><span>Active numbers</span><strong>1,284</strong></div><div className="mini-metric"><span>Avg. latency</span><strong>840ms</strong></div><div className="mini-metric"><span>Countries</span><strong>{stats.data?.data.countries || 42}</strong></div></div></div></div><div className="absolute -bottom-7 -left-8 hidden w-52 rounded-2xl border border-white/10 bg-[#10253c] p-4 shadow-2xl sm:block"><div className="flex items-center gap-2 text-xs font-bold text-emerald-300"><span className="status-dot" /> Activation success</div><div className="mt-3 font-mono text-sm text-white">+62 •••• •••• 42</div><div className="mt-1 text-xs text-slate-500">whatsapp · 1.2s ago</div></div></div></div></section>
    <section id="platform" className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><div className="grid gap-8 lg:grid-cols-[.85fr_1.15fr] lg:items-end"><div><p className="section-kicker">The operating layer</p><h2 className="section-title">Everything between your app and the network.</h2></div><p className="max-w-xl text-base leading-7 text-slate-500">webotp keeps the operational details in one place: inventory, pricing, supplier health, wallet movements, activations, and developer access.</p></div><div className="mt-12 grid gap-4 md:grid-cols-2 lg:grid-cols-4"><Feature icon={Server} title="Supplier router" text="Choose a route using stock, price, availability, success rate, latency, and priority." /><Feature icon={Wallet} title="Ledger wallet" text="Every deposit, debit, and refund has a unique reference and an auditable trail." /><Feature icon={KeyRound} title="Secure keys" text="Secrets are hashed at rest and shown only once. Rate limits and IP allowlists included." /><Feature icon={Webhook} title="Event delivery" text="Signed, timestamped, idempotent webhooks with logs, retries, and test delivery." /></div></section>
    <section id="services" className="border-y border-slate-200 bg-white"><div className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="section-kicker">Network catalog</p><h2 className="section-title">One catalog. Multiple routes.</h2><p className="mt-4 max-w-xl text-slate-500">Pricing, availability, and stock are designed to be managed from the database—not buried inside application code.</p></div><a href="#developers" className="text-sm font-bold text-cyan-700">View API reference <ArrowRight size={15} className="ml-1 inline" /></a></div><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{services.slice(0, 5).map(service => <div className="service-card" key={service.id}><div className="service-icon">{service.name.slice(0, 1)}</div><div className="mt-5 flex items-start justify-between gap-2"><div><h3 className="font-extrabold">{service.name}</h3><p className="mt-1 text-xs text-slate-400">{service.countryName} · {service.operator}</p></div><span className={service.availability === "available" ? "pill-green" : "pill-amber"}>{service.availability}</span></div><div className="mt-6 flex items-end justify-between"><div><p className="text-xs text-slate-400">From</p><p className="mt-1 text-lg font-black">{formatIDR(service.salePriceMinor)}</p></div><div className="text-right"><p className="text-xs text-slate-400">Stock</p><p className="mt-1 font-bold text-slate-700">{service.stock}</p></div></div></div>)}{services.length === 0 && <div className="col-span-full rounded-2xl border border-dashed border-slate-300 p-10 text-center text-slate-500">Catalog is syncing. Configure your first supplier to publish services.</div>}</div></div></section>
    <section id="developers" className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><div className="developer-panel"><div className="grid gap-10 lg:grid-cols-[.8fr_1.2fr] lg:items-center"><div><p className="section-kicker text-cyan-300">Built for developers</p><h2 className="mt-3 text-4xl font-black tracking-tight text-white">A predictable API surface.</h2><p className="mt-5 leading-7 text-slate-300">Consistent JSON responses, correct HTTP status codes, idempotency keys for money-moving operations, and webhooks for state changes.</p><div className="mt-7 flex flex-wrap gap-3"><button className="btn-primary" onClick={() => startLogin()}>Create workspace <ArrowRight size={16} /></button><a href="/api/v1/services" className="btn-ghost-dark"><ExternalLink size={15} /> Try public endpoint</a></div></div><div className="code-block"><div className="code-head"><span>POST /api/v1/number/buy</span><Copy size={14} /></div><pre>{`{\n  "success": true,\n  "data": {\n    "activationId": "act_8f2d...",\n    "state": "PENDING",\n    "amount": 1500\n  },\n  "message": "Activation created",\n  "error": null\n}`}</pre></div></div></div></section>
    <section id="status" className="border-y border-slate-200 bg-white"><div className="mx-auto grid max-w-7xl gap-6 px-5 py-12 sm:grid-cols-4 lg:px-8"><Stat value={stats.data?.data.services || "—"} label="Active services" /><Stat value={stats.data?.data.countries || "—"} label="Countries" /><Stat value={stats.data?.data.suppliers || "—"} label="Supplier routes" /><Stat value={stats.data?.data.uptime || "99.95%"} label="Platform uptime" /></div></section>
    <section id="faq" className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><div className="grid gap-12 lg:grid-cols-[.75fr_1.25fr]"><div><p className="section-kicker">Questions, answered</p><h2 className="section-title">Designed for the next stage of your stack.</h2></div><div className="space-y-3"><Faq q="How are API keys protected?" a="The complete secret is shown only at creation or regeneration. The database stores a SHA-256 hash and a short key identifier." /><Faq q="How do transactions avoid double debits?" a="Buy operations require an idempotency key and use atomic wallet updates plus a unique ledger reference." /><Faq q="Can I add more suppliers later?" a="Yes. Supplier adapters implement SupplierInterface while routing decisions remain in the core service layer." /></div></div></section>
  </main><footer className="border-t border-slate-200 bg-[#f7f9fc]"><div className="mx-auto flex max-w-7xl flex-col gap-4 px-5 py-8 text-sm text-slate-500 sm:flex-row sm:items-center sm:justify-between lg:px-8"><Logo /><span>© 2026 webotp. API infrastructure for verification products.</span></div></footer></div>;
}

function Feature({ icon: Icon, title, text }: { icon: typeof Server; title: string; text: string }) { return <div className="feature-card"><div className="feature-icon"><Icon size={19} /></div><h3 className="mt-5 font-extrabold">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{text}</p></div>; }
function Stat({ value, label }: { value: string | number; label: string }) { return <div><p className="text-3xl font-black tracking-tight text-slate-950">{value}</p><p className="mt-1 text-sm text-slate-500">{label}</p></div>; }
function Faq({ q, a }: { q: string; a: string }) { const [open, setOpen] = useState(false); return <button className="w-full rounded-2xl border border-slate-200 bg-slate-50 px-5 py-4 text-left" onClick={() => setOpen(!open)}><div className="flex items-center justify-between gap-4 font-bold"><span>{q}</span><ChevronRight size={17} className={`transition ${open ? "rotate-90" : ""}`} /></div>{open && <p className="mt-3 max-w-2xl text-sm leading-6 text-slate-500">{a}</p>}</button>; }

function AppShell({ user, onLogout }: { user: NonNullable<ReturnType<typeof useAuth>["user"]>; onLogout: () => void }) {
  const [active, setActive] = useState("dashboard");
  const [mobileOpen, setMobileOpen] = useState(false);
  const overview = trpc.dashboard.overview.useQuery(undefined);
  const keys = trpc.apiKeys.list.useQuery(undefined, { enabled: active === "keys" });
  const webhooks = trpc.webhooks.list.useQuery(undefined, { enabled: active === "webhooks" });
  const catalog = trpc.public.catalog.useQuery();
  const createKey = trpc.apiKeys.create.useMutation({ onSuccess: (result) => { keys.refetch(); if (result.data?.secret) { navigator.clipboard?.writeText(result.data.secret); toast.success("API key created. Secret copied once."); } } });
  const revokeKey = trpc.apiKeys.revoke.useMutation({ onSuccess: () => { keys.refetch(); toast.success("API key revoked"); } });
  const createWebhook = trpc.webhooks.create.useMutation({ onSuccess: (result) => { webhooks.refetch(); toast.success(result.data?.secret ? "Webhook created. Secret shown once." : "Webhook created"); } });
  const testWebhook = trpc.webhooks.test.useMutation({ onSuccess: () => toast.success("Test webhook queued") });
  const purchase = trpc.dashboard.purchase.useMutation({ onSuccess: () => { overview.refetch(); toast.success("Activation created and wallet ledger updated"); }, onError: (error) => toast.error(error.message) });
  const [keyName, setKeyName] = useState("");
  const [webhookUrl, setWebhookUrl] = useState("");
  const [webhookEvents, setWebhookEvents] = useState("activation.success,refund.created");
  const [showSecret, setShowSecret] = useState<string | null>(null);
  const current = navItems.find(item => item.key === active) ?? navItems[0];
  const wallet = overview.data?.data.wallet;
  const activations = overview.data?.data.activations ?? [];
  const transactions = overview.data?.data.transactions ?? [];
  const services = catalog.data?.data ?? [];
  const setPage = (page: string) => { setActive(page); setMobileOpen(false); };
  const createApiKey = async () => { if (!keyName.trim()) return toast.error("Give this key a name first"); const result = await createKey.mutateAsync({ name: keyName.trim(), rateLimitPerMinute: 60 }); if (result.data?.secret) setShowSecret(result.data.secret); setKeyName(""); };
  const createWh = async () => { if (!webhookUrl.trim()) return toast.error("Enter a valid webhook URL"); const result = await createWebhook.mutateAsync({ url: webhookUrl.trim(), events: webhookEvents.split(",").map(v => v.trim()).filter(Boolean) }); if (result.data?.secret) setShowSecret(result.data.secret); setWebhookUrl(""); };
  return <div className="min-h-screen bg-[#f4f7fb] text-slate-950"><aside className={`fixed inset-y-0 left-0 z-50 flex w-[260px] flex-col border-r border-slate-200 bg-white transition-transform lg:translate-x-0 ${mobileOpen ? "translate-x-0" : "-translate-x-full"}`}><div className="flex h-20 items-center border-b border-slate-100 px-6"><Logo /></div><div className="px-4 pt-6"><p className="nav-label">Workspace</p><nav className="mt-3 space-y-1">{navItems.filter(item => item.key !== "admin" || user.role === "admin").map(item => <button key={item.key} onClick={() => item.key === "admin" ? window.location.assign("/admin") : setPage(item.key)} className={`side-link ${active === item.key ? "side-link-active" : ""}`}><item.icon size={17} />{item.label}</button>)}</nav></div><div className="mt-auto border-t border-slate-100 p-4"><div className="mb-3 rounded-2xl bg-[#071a2d] p-4 text-white"><div className="flex items-center justify-between"><span className="text-[10px] font-bold uppercase tracking-widest text-slate-400">Wallet balance</span><Wallet size={16} className="text-cyan-300" /></div><p className="mt-2 text-xl font-black">{formatIDR(wallet?.balanceMinor)}</p><button onClick={() => setPage("wallet")} className="mt-3 flex items-center gap-1 text-xs font-bold text-cyan-300">Add funds <ArrowRight size={13} /></button></div><button onClick={onLogout} className="side-link text-slate-500"><LogOut size={16} /> Sign out</button></div></aside><div className="lg:pl-[260px]"><header className="sticky top-0 z-30 flex h-20 items-center justify-between border-b border-slate-200 bg-white/90 px-5 backdrop-blur-xl lg:px-8"><div className="flex items-center gap-3"><button className="rounded-xl border border-slate-200 p-2 lg:hidden" onClick={() => setMobileOpen(!mobileOpen)}>{mobileOpen ? <X size={18} /> : <Menu size={18} />}</button><div><p className="text-xs font-bold uppercase tracking-[.18em] text-slate-400">Workspace / {current.label}</p><h1 className="mt-1 text-lg font-black">{current.label}</h1></div></div><div className="flex items-center gap-4"><div className="hidden rounded-full border border-emerald-200 bg-emerald-50 px-3 py-1.5 text-xs font-bold text-emerald-700 sm:flex sm:items-center sm:gap-2"><span className="status-dot bg-emerald-500" /> Systems operational</div><div className="flex items-center gap-3 border-l border-slate-200 pl-4"><div className="avatar">{(user.name || user.email || "U").slice(0, 1).toUpperCase()}</div><div className="hidden sm:block"><p className="text-sm font-bold">{user.name || "Workspace owner"}</p><p className="text-xs text-slate-400">{user.email || "Authenticated account"}</p></div></div></div></header><main className="mx-auto max-w-[1500px] p-5 lg:p-8">{active === "dashboard" && <Dashboard wallet={wallet} overview={overview.data?.data} setPage={setPage} />}{active === "buy" && <BuyPage services={services} onBuy={(serviceId) => purchase.mutate({ serviceId, idempotencyKey: `idem_${crypto.randomUUID()}` })} pending={purchase.isPending} />}{active === "activations" && <ActivationsPage activations={activations} />}{active === "wallet" && <WalletPage wallet={wallet} transactions={transactions} setPage={setPage} />}{active === "keys" && <KeysPage keys={keys.data?.data ?? []} keyName={keyName} setKeyName={setKeyName} createApiKey={createApiKey} revokeKey={(id) => revokeKey.mutate({ id })} loading={createKey.isPending} />}{active === "webhooks" && <WebhooksPage webhooks={webhooks.data?.data ?? []} url={webhookUrl} setUrl={setWebhookUrl} events={webhookEvents} setEvents={setWebhookEvents} createWebhook={createWh} testWebhook={(id) => testWebhook.mutate({ endpointId: id })} loading={createWebhook.isPending} />}{active === "docs" && <DocsPage />}</main></div>{showSecret && <SecretModal secret={showSecret} onClose={() => setShowSecret(null)} />}</div>;
}

function Dashboard({ wallet, overview, setPage }: { wallet: any; overview: any; setPage: (page: string) => void }) { return <div className="space-y-8"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="section-kicker">Good to see you</p><h2 className="mt-2 text-3xl font-black tracking-tight">Your control room.</h2><p className="mt-2 text-slate-500">Monitor activations, wallet movements, and API health from one place.</p></div><button onClick={() => setPage("buy")} className="btn-primary"><Plus size={17} /> Buy a number</button></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Available balance" value={formatIDR(wallet?.balanceMinor)} icon={Wallet} tone="cyan" meta="Ledger-backed" /><MetricCard label="Total activations" value={overview?.totalActivations ?? 0} icon={Zap} tone="violet" meta="All time" /><MetricCard label="Active now" value={overview?.activeActivations ?? 0} icon={Play} tone="amber" meta="Awaiting OTP" /><MetricCard label="API requests" value="0" icon={BarChart3} tone="emerald" meta="This period" /></div><div className="grid gap-6 xl:grid-cols-[1.4fr_.8fr]"><div className="panel overflow-hidden"><div className="panel-head"><div><h3 className="font-black">Recent activations</h3><p className="mt-1 text-xs text-slate-400">State changes are always recorded.</p></div><button onClick={() => setPage("activations")} className="text-xs font-bold text-cyan-700">View all <ArrowRight size={13} className="ml-1 inline" /></button></div><div className="divide-y divide-slate-100">{(overview?.activations ?? []).slice(0, 5).map((item: any) => <ActivationRow key={item.id} item={item} />)}{!(overview?.activations ?? []).length && <EmptyState icon={Zap} title="No activations yet" text="Your first number will appear here after a successful reserve." action="Browse catalog" onClick={() => setPage("buy")} />}</div></div><div className="panel"><div className="panel-head"><div><h3 className="font-black">Network pulse</h3><p className="mt-1 text-xs text-slate-400">Your provider health overview</p></div><MoreHorizontal size={18} className="text-slate-400" /></div><div className="space-y-5 p-5"><Pulse label="Supplier availability" value="99.2%" color="bg-emerald-500" /><Pulse label="Average response" value="840ms" color="bg-cyan-500" /><Pulse label="Webhook delivery" value="100%" color="bg-violet-500" /><div className="rounded-2xl bg-slate-50 p-4"><div className="flex items-start gap-3"><ShieldCheck size={18} className="mt-0.5 text-emerald-600" /><div><p className="text-sm font-bold">Production safeguards on</p><p className="mt-1 text-xs leading-5 text-slate-500">Idempotency, atomic ledger updates, and signed events are enabled.</p></div></div></div></div></div></div></div>; }
function MetricCard({ label, value, icon: Icon, tone, meta }: { label: string; value: string | number; icon: typeof Wallet; tone: string; meta: string }) { return <div className="metric-card"><div className={`metric-icon ${tone}`}><Icon size={18} /></div><p className="mt-5 text-xs font-semibold text-slate-400">{label}</p><p className="mt-1 text-2xl font-black tracking-tight">{value}</p><p className="mt-2 text-xs text-slate-400">{meta}</p></div>; }
function Pulse({ label, value, color }: { label: string; value: string; color: string }) { return <div><div className="mb-2 flex justify-between text-xs font-bold"><span className="text-slate-500">{label}</span><span>{value}</span></div><div className="h-2 rounded-full bg-slate-100"><div className={`h-2 w-[88%] rounded-full ${color}`} /></div></div>; }
function ActivationRow({ item }: { item: any }) { const stateTone = ["SUCCESS", "OTP_RECEIVED"].includes(item.state) ? "pill-green" : ["CANCELLED", "TIMEOUT", "REFUND"].includes(item.state) ? "pill-red" : "pill-amber"; return <div className="flex items-center justify-between gap-4 px-5 py-4"><div className="flex min-w-0 items-center gap-3"><div className="service-icon small">{item.phoneMasked?.slice(0, 1) || "#"}</div><div className="min-w-0"><p className="truncate text-sm font-bold">{item.activationId}</p><p className="mt-1 text-xs text-slate-400">{item.phoneMasked || "Number reserved"} · {formatDate(item.createdAt)}</p></div></div><span className={stateTone}>{item.state}</span></div>; }
function EmptyState({ icon: Icon, title, text, action, onClick }: { icon: typeof Zap; title: string; text: string; action: string; onClick: () => void }) { return <div className="p-10 text-center"><div className="mx-auto flex h-12 w-12 items-center justify-center rounded-2xl bg-slate-100 text-slate-400"><Icon size={20} /></div><p className="mt-4 font-bold">{title}</p><p className="mt-1 text-sm text-slate-400">{text}</p><button onClick={onClick} className="mt-5 text-sm font-bold text-cyan-700">{action} <ArrowRight size={14} className="ml-1 inline" /></button></div>; }

function BuyPage({ services, onBuy, pending }: { services: any[]; onBuy: (id: number) => void; pending: boolean }) { return <div className="space-y-7"><div><p className="section-kicker">Live catalog</p><h2 className="mt-2 text-3xl font-black tracking-tight">Choose your route.</h2><p className="mt-2 text-slate-500">Prices and stock are database-driven and routed by supplier health.</p></div><div className="flex flex-wrap gap-3"><div className="filter-chip active">All services</div><div className="filter-chip">Indonesia · +62</div><div className="filter-chip">Any operator</div><div className="filter-chip">Sort: recommended</div></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{services.map(service => <div className="panel p-5" key={service.id}><div className="flex items-start justify-between"><div className="flex items-center gap-3"><div className="service-icon">{service.name.slice(0, 1)}</div><div><h3 className="font-black">{service.name}</h3><p className="mt-1 text-xs text-slate-400">{service.countryName} · {service.operator}</p></div></div><span className={service.availability === "available" ? "pill-green" : "pill-amber"}>{service.availability}</span></div><div className="mt-7 grid grid-cols-3 gap-3 border-y border-slate-100 py-4"><div><p className="text-[10px] font-bold uppercase tracking-widest text-slate-400">Price</p><p className="mt-1 font-black">{formatIDR(service.salePriceMinor)}</p></div><div><p className="text-[10px] font-bold uppercase tracking-widest text-slate-400">Stock</p><p className="mt-1 font-black">{service.stock}</p></div><div><p className="text-[10px] font-bold uppercase tracking-widest text-slate-400">Success</p><p className="mt-1 font-black">{(service.successRateBps / 100).toFixed(1)}%</p></div></div><div className="mt-4 flex items-center justify-between"><p className="text-xs text-slate-400">{service.responseTimeMs}ms avg response</p><button disabled={pending || service.stock < 1} onClick={() => onBuy(service.id)} className="btn-primary px-4 py-2 text-xs disabled:cursor-not-allowed disabled:opacity-50">{pending ? "Reserving..." : "Reserve number"}</button></div></div>)}{!services.length && <div className="panel col-span-full p-12 text-center text-slate-500">No active catalog entries. Add a supplier and publish a service to continue.</div>}</div></div>; }
function ActivationsPage({ activations }: { activations: any[] }) { return <div className="space-y-7"><div><p className="section-kicker">State machine</p><h2 className="mt-2 text-3xl font-black tracking-tight">Activation history.</h2><p className="mt-2 text-slate-500">Every transition is persisted from CREATED through SUCCESS or REFUND.</p></div><div className="panel overflow-hidden"><div className="overflow-x-auto"><table className="data-table"><thead><tr><th>Activation</th><th>Phone</th><th>Amount</th><th>State</th><th>Created</th></tr></thead><tbody>{activations.map(item => <tr key={item.id}><td className="font-mono text-xs">{item.activationId}</td><td>{item.phoneMasked || "—"}</td><td>{formatIDR(item.amountMinor)}</td><td><span className={item.state === "SUCCESS" ? "pill-green" : "pill-amber"}>{item.state}</span></td><td>{formatDate(item.createdAt)}</td></tr>)}{!activations.length && <tr><td colSpan={5} className="py-16 text-center text-slate-400">No activation records.</td></tr>}</tbody></table></div></div></div>; }
function WalletPage({ wallet, transactions, setPage }: { wallet: any; transactions: any[]; setPage: (page: string) => void }) { const deposit = trpc.wallet.createDeposit.useMutation({ onSuccess: result => toast.success(`Deposit ${result.data?.reference ?? "created"} is pending`) }); const create = () => { const input = window.prompt("Deposit amount in IDR (minimum 10,000)", "10000"); const amount = Number(input); if (Number.isInteger(amount) && amount >= 10000) deposit.mutate({ amountMinor: amount }); else if (input !== null) toast.error("Enter a valid amount of at least Rp 10.000"); }; return <div className="space-y-7"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="section-kicker">Money movement</p><h2 className="mt-2 text-3xl font-black tracking-tight">Wallet & ledger.</h2><p className="mt-2 text-slate-500">The balance is a projection of immutable transaction entries.</p></div><button onClick={create} disabled={deposit.isPending} className="btn-primary"><Plus size={17} /> {deposit.isPending ? "Creating..." : "Create deposit"}</button></div><div className="wallet-hero"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-cyan-200">Available balance</p><p className="mt-3 text-4xl font-black">{formatIDR(wallet?.balanceMinor)}</p><p className="mt-2 text-sm text-slate-300">Wallet #{wallet?.id ?? "—"} · {wallet?.currency ?? "IDR"}</p></div><div className="rounded-2xl border border-white/10 bg-white/5 p-4"><ShieldCheck size={22} className="text-emerald-300" /><p className="mt-3 text-sm font-bold text-white">Ledger protected</p><p className="mt-1 text-xs text-slate-400">Atomic debit + unique reference</p></div></div><div className="panel overflow-hidden"><div className="panel-head"><div><h3 className="font-black">Recent mutations</h3><p className="mt-1 text-xs text-slate-400">Every row has a unique transaction reference.</p></div></div><div className="overflow-x-auto"><table className="data-table"><thead><tr><th>Reference</th><th>Description</th><th>Direction</th><th>Amount</th><th>Balance after</th><th>Time</th></tr></thead><tbody>{transactions.map(tx => <tr key={tx.id}><td className="font-mono text-xs">{tx.reference}</td><td>{tx.description}</td><td><span className={tx.direction === "credit" ? "pill-green" : "pill-red"}>{tx.direction}</span></td><td className="font-bold">{tx.direction === "credit" ? "+" : "-"}{formatIDR(tx.amountMinor)}</td><td>{formatIDR(tx.balanceAfterMinor)}</td><td>{formatDate(tx.createdAt)}</td></tr>)}{!transactions.length && <tr><td colSpan={6} className="py-16 text-center text-slate-400">Ledger is empty. Create your first deposit.</td></tr>}</tbody></table></div></div></div>; }
function KeysPage({ keys, keyName, setKeyName, createApiKey, revokeKey, loading }: { keys: any[]; keyName: string; setKeyName: (v: string) => void; createApiKey: () => void; revokeKey: (id: number) => void; loading: boolean }) { return <div className="space-y-7"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="section-kicker">Developer access</p><h2 className="mt-2 text-3xl font-black tracking-tight">API keys.</h2><p className="mt-2 text-slate-500">Secrets are hashed at rest and never shown again after creation.</p></div></div><div className="panel p-5"><div className="flex flex-col gap-3 sm:flex-row"><input className="input flex-1" value={keyName} onChange={e => setKeyName(e.target.value)} placeholder="Key name · Production, Testing, Website A" /><button onClick={createApiKey} disabled={loading} className="btn-primary whitespace-nowrap"><Plus size={16} /> {loading ? "Creating..." : "Create API key"}</button></div></div><div className="grid gap-4">{keys.map(key => <div className="panel flex flex-col justify-between gap-5 p-5 sm:flex-row sm:items-center" key={key.id}><div className="flex items-center gap-4"><div className="feature-icon"><KeyRound size={19} /></div><div><div className="flex flex-wrap items-center gap-2"><h3 className="font-black">{key.name}</h3><span className={key.status === "active" ? "pill-green" : "pill-red"}>{key.status}</span></div><p className="mt-2 font-mono text-xs text-slate-500">{key.keyPrefix}••••••••••••</p><p className="mt-1 text-xs text-slate-400">{key.requestCount} requests · {key.rateLimitPerMinute}/min · Last used {formatDate(key.lastUsedAt)}</p></div></div><button onClick={() => revokeKey(key.id)} disabled={key.status !== "active"} className="btn-danger">Revoke</button></div>)}{!keys.length && <div className="panel p-12 text-center text-slate-500">No API keys yet. Create one for your first integration.</div>}</div></div>; }
function WebhooksPage({ webhooks, url, setUrl, events, setEvents, createWebhook, testWebhook, loading }: { webhooks: any[]; url: string; setUrl: (v: string) => void; events: string; setEvents: (v: string) => void; createWebhook: () => void; testWebhook: (id: number) => void; loading: boolean }) { return <div className="space-y-7"><div><p className="section-kicker">Event delivery</p><h2 className="mt-2 text-3xl font-black tracking-tight">Webhooks.</h2><p className="mt-2 text-slate-500">Signed events for activation and refund state changes, with idempotent delivery records.</p></div><div className="panel p-5"><div className="grid gap-3 lg:grid-cols-[1.2fr_1fr_auto]"><input className="input" value={url} onChange={e => setUrl(e.target.value)} placeholder="https://your-app.com/webhooks/webotp" /><input className="input" value={events} onChange={e => setEvents(e.target.value)} placeholder="activation.success,refund.created" /><button onClick={createWebhook} disabled={loading} className="btn-primary whitespace-nowrap"><Plus size={16} /> Add endpoint</button></div></div><div className="grid gap-4">{webhooks.map(webhook => <div className="panel p-5" key={webhook.id}><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><div className="flex items-center gap-2"><span className="status-dot bg-emerald-500" /><h3 className="font-black">{webhook.url}</h3><span className="pill-green">{webhook.status}</span></div><p className="mt-2 text-xs text-slate-400">Events: {JSON.parse(webhook.events).join(" · ")} · Last delivery {formatDate(webhook.lastDeliveryAt)}</p></div><button onClick={() => testWebhook(webhook.id)} className="btn-secondary"><RefreshCw size={15} /> Test webhook</button></div></div>)}{!webhooks.length && <div className="panel p-12 text-center text-slate-500">No webhook endpoints yet.</div>}</div></div>; }
function DocsPage() { return <div className="space-y-7"><div><p className="section-kicker">Developer reference</p><h2 className="mt-2 text-3xl font-black tracking-tight">API documentation.</h2><p className="mt-2 text-slate-500">Every response follows the same contract, with idempotency for money-moving calls.</p></div><div className="grid gap-6 xl:grid-cols-[.8fr_1.2fr]"><div className="panel p-5"><p className="text-xs font-bold uppercase tracking-widest text-slate-400">Authentication</p><div className="mt-4 rounded-xl bg-[#071a2d] p-4 font-mono text-xs text-cyan-200">X-API-Key: wotp_••••••••</div><div className="mt-6 space-y-3 text-sm text-slate-600"><p className="flex gap-2"><Check size={16} className="mt-0.5 text-emerald-500" /> JSON-only responses</p><p className="flex gap-2"><Check size={16} className="mt-0.5 text-emerald-500" /> Correct HTTP status codes</p><p className="flex gap-2"><Check size={16} className="mt-0.5 text-emerald-500" /> Request rate limits</p><p className="flex gap-2"><Check size={16} className="mt-0.5 text-emerald-500" /> Idempotency-Key on buy</p></div></div><div className="code-block light"><div className="code-head"><span>GET /api/v1/balance</span><span className="pill-green">200 OK</span></div><pre>{`{\n  "success": true,\n  "data": {\n    "balanceMinor": 0,\n    "currency": "IDR"\n  },\n  "message": "Success",\n  "error": null\n}`}</pre></div></div><div className="panel overflow-hidden"><table className="data-table"><thead><tr><th>Method</th><th>Endpoint</th><th>Purpose</th></tr></thead><tbody>{[["GET", "/api/v1/balance", "Read wallet balance"], ["GET", "/api/v1/services", "List live catalog"], ["GET", "/api/v1/availability", "Read stock and availability"], ["POST", "/api/v1/number/buy", "Reserve a number"], ["GET", "/api/v1/activation/{id}", "Read activation state"], ["POST", "/api/v1/activation/{id}/cancel", "Cancel pending activation"], ["GET", "/api/v1/history", "Read activation history"]].map(([method, endpoint, purpose]) => <tr key={endpoint}><td><span className="method-pill">{method}</span></td><td className="font-mono text-xs">{endpoint}</td><td>{purpose}</td></tr>)}</tbody></table></div></div>; }
function SecretModal({ secret, onClose }: { secret: string; onClose: () => void }) { return <div className="fixed inset-0 z-[70] flex items-center justify-center bg-slate-950/60 p-5 backdrop-blur-sm"><div className="w-full max-w-lg rounded-3xl bg-white p-6 shadow-2xl"><div className="flex items-start justify-between"><div><div className="feature-icon"><ShieldCheck size={19} /></div><h3 className="mt-5 text-xl font-black">Save this secret now</h3><p className="mt-2 text-sm leading-6 text-slate-500">This is the only time the complete secret will be shown. Store it in your password manager.</p></div><button onClick={onClose} className="rounded-xl p-2 text-slate-400 hover:bg-slate-100"><X size={18} /></button></div><div className="mt-6 flex items-center gap-2 rounded-2xl bg-slate-950 p-4"><code className="min-w-0 flex-1 break-all text-xs text-cyan-200">{secret}</code><button onClick={() => { navigator.clipboard?.writeText(secret); toast.success("Secret copied"); }} className="rounded-lg bg-white/10 p-2 text-white"><Copy size={15} /></button></div><button onClick={onClose} className="btn-primary mt-5 w-full justify-center">I saved it</button></div></div>; }

export default function Home() { const auth = useAuth(); if (auth.loading) return <div className="flex min-h-screen items-center justify-center bg-[#f7f9fc]"><div className="loading-ring" /></div>; if (auth.isAuthenticated && auth.user) return <AppShell user={auth.user} onLogout={auth.logout} />; return <PublicHome />; }
